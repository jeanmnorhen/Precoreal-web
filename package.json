{
  "name": "precoreal-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Preço Real Monorepo (Next.js, NestJS, Expo)",
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "turbo": "^2.0.4",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@sentry/nextjs": "^10.57.0",
    "@sentry/node": "^10.57.0",
    "@types/leaflet": "^1.9.21",
    "leaflet": "^1.9.4",
    "prom-client": "^15.1.3"
  }
}
